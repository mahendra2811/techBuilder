# Backend ORIGIN only — the /api/v1 prefix comes from the frozen contracts (API_BASE).
# Server-side only (tokens never reach client JS, so the browser never needs this).
BACKEND_ORIGIN=http://localhost:4000
