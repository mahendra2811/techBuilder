import { AccountantVerifyScreen } from '@/components/screens/accountant-verify-screen';

// Auth + role pinning + shell come from the parent accountant/layout.tsx.
export default function Page() {
  return <AccountantVerifyScreen />;
}
