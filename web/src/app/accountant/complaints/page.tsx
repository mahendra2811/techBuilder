import { ComplaintScreen } from '@/components/screens/complaint-screen';

// Auth + role pinning + shell now come from the parent accountant/layout.tsx (CW-3).
export default function Page() {
  return <ComplaintScreen />;
}
