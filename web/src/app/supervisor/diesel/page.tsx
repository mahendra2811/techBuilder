import { DieselScreen } from '@/components/screens/diesel-screen';

// Auth + role pinning + shell come from the parent supervisor/layout.tsx.
export default function Page() {
  return <DieselScreen />;
}
