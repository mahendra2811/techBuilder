import { SupervisorDashboardScreen } from '@/components/screens/supervisor-dashboard-screen';

// Auth + role pinning + shell come from the parent supervisor/layout.tsx.
export default function Page() {
  return <SupervisorDashboardScreen />;
}
