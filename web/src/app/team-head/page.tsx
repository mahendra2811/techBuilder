import { TeamHeadDashboardScreen } from '@/components/screens/team-head-dashboard-screen';

// Auth + role pinning + shell come from the parent team-head/layout.tsx.
export default function Page() {
  return <TeamHeadDashboardScreen />;
}
