import { ComplaintScreen } from '@/components/screens/complaint-screen';

// Auth + role pinning + shell come from the parent worker/layout.tsx.
export default function Page() {
  return <ComplaintScreen />;
}
